server:
  port: 9001
 
spring:
  application:
    name: user-service
    
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true #유레카 서버에서 인스턴스들의 갱신된 정보를 주기적으로 가져올 것인지
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka #유레카 서버의 위치